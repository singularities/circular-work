<div class="tasks">
  <h1>
    {{ t 'tasks.index.title'}}

    {{#if model }}
      {{#link-to 'tasks.new' class="btn btn-primary pull-right"}}
        {{t 'tasks.index.new.button'}}
      {{/link-to}}
    {{/if}}
  </h1>

  {{task-list tasks=model}}
</div>
