<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :recurrence %>
    <%= f.number_field :recurrence %>
  </div>

  <div class="field">
    <%= f.label :recurrence_match %>
    <%= f.text_field :recurrence_match %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :notification_subject %>
    <%= f.text_field :notification_subject %>
  </div>

  <div class="field">
    <%= f.label :notification_body %>
    <%= f.text_area :notification_body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
