<style>
  body {
    margin: 0;
  }

  .container {
    max-width: 1170px;
    margin: auto;
  }

  a {
    color: #47afbe;
    text-decoration: none;
  }

  .btn {
    display: inline-block;
    margin-bottom: 0;
    font-weight: normal;
    text-align: center;
    vertical-align: middle;
    touch-action: manipulation;
    cursor: pointer;
    background-image: none;
    border: 1px solid transparent;
    white-space: nowrap;
    padding: 6px 12px;
    font-size: 14px;
    line-height: 1.42857143;
    border-radius: 4px;
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
  }

  .btn-lg {
    padding: 10px 16px;
    font-size: 18px;
    line-height: 1.3333333;
    border-radius: 6px;
  }

  .btn-accent {
    color: white;
    background-color: #e74f3a;
    border-color: #c22c18;
    text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.2);
    -webkit-box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15), 0 1px 1px rgba(0, 0, 0, 0.075);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15), 0 1px 1px rgba(0, 0, 0, 0.075);
    background-image: -webkit-linear-gradient(top, #e74f3a 0%, #cb2e19 100%);
    background-image: -o-linear-gradient(top, #e74f3a 0%, #cb2e19 100%);
    background-image: linear-gradient(to bottom, #e74f3a 0%, #cb2e19 100%);
    filter: progid:DXImageTransform.Microsoft.gradient(startColorstr='#ffe74f3a', endColorstr='#ffcb2e19', GradientType=0);
    filter: progid:DXImageTransform.Microsoft.gradient(enabled = false);
    background-repeat: repeat-x;
  }

  .btn-accent:hover {
    background-color: #cb2e19;
    background-position: 0 -15px;
    border-color: #b42916;
  }

  .float-right {
    float: right;
  }

  footer {
    background-image: linear-gradient(to bottom, #ffffff 0%, #f8f8f8 100%);
    background-repeat: repeat-x;
    border-radius: 4px;
    background-color: #f8f8f8;
    border-color: #e7e7e7;
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15), 0 1px 5px rgba(0, 0, 0, 0.075);
    margin-top: 15px;
    min-height: 50px;
  }

  footer .message {
    color: #47afbe;
    font-size: 16px;
    margin-bottom: 10px;
  }

  footer .btn {
    margin-bottom: 10px;
  }
</style>
