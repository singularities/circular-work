<div class="session-register">
  <!--
  {{#if errorMessage}}
    <p class="alert alert-danger">{{errorMessage}}</p>
  {{/if}}

  {{#bs-form action="authenticate" submitOnEnter=true}}
    {{bs-form-element label=(t 'session.email.label')
                      controlType="email"
                      placeholder=(t 'session.email.placeholder')
                      value=identification}}
    {{#bs-button type="accent" block="true" action="authenticate"}}
      {{ t 'session.password_forgotten.button'}}
    {{/bs-button}}
  {{/bs-form}}
  -->
  {{#bs-alert type="info" dismissible=false}}
    <p>
      Aún estamos en periodo de pruebas.
    </p>
    <p>
      Si quieres recuperar tu contraseña, ponte en contacto con nosotros en <a href="mailto:info@circular.work">info@circular.work</a>
    </p>
  {{/bs-alert}}
</div>
